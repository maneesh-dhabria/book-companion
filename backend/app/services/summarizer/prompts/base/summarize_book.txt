You are creating a book-level summary of "{{ book_title }}" by {{ author }}.
This summary synthesizes {{ section_count }} section summaries below.

{% include "fragments/audience/" + audience + ".txt" %}
{% include "fragments/content_focus/" + content_focus + ".txt" %}
{% include "fragments/style/" + style + ".txt" %}
{% include "fragments/compression/" + compression + ".txt" %}

## Section summaries
{% for section in sections %}
### {{ section.title }}
{{ section.summary }}
{% endfor %}

{% if existing_tags %}
## Existing tags for this book
The user has already applied these tags: {{ existing_tags | join(", ") }}.
Do not re-suggest anything that overlaps with the existing tags.
{% endif %}

## Output format
Return JSON with:
- `summary_md` (string): the book summary as markdown.
- `suggested_tags` (array of up to 7 short strings, each <= 40 chars): new
  tags we should propose to the user. Prefer single or two-word labels that
  capture the dominant topics, methodologies, or frameworks in the book.
  Return `[]` if nothing stands out.

