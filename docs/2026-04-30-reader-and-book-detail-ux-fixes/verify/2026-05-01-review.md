# /verify Review — Reader + Book Detail UX Fixes (2026-05-01)

**Branch:** `feature/reader-bookdetail-ux-fixes`
**Worktree:** `.worktrees/reader-bookdetail-ux-fixes`
**Tip:** `721cfb1`
**Plan:** `docs/plans/2026-04-30-reader-and-book-detail-ux-fixes-implementation-plan.md`
**Spec:** `docs/specs/2026-04-30-reader-and-book-detail-ux-fixes-spec.md`
**Requirements:** `docs/requirements/2026-04-30-reader-and-book-detail-ux-fixes.md`

## Verdict: **GO**

23 commits across 19 plan tasks (B1-B8, F1-F10) + final verification + 3 verify-pass fixes. All static checks clean on the changed slice; full regression suites green; all 8 spec §12.6 bug verifications resolved; Playwright walkthrough of spec §12.5 user journeys passes without console errors.

---

## Phase 2 — Static Verification

| Check | Scope | Result |
|------|-------|--------|
| `ruff check` | 18 changed `.py` files | **clean** (1 pre-existing N814 in `books.py:296` from commit 78c076d, April 23 — outside this branch's slice) |
| `ruff format --check` | 18 changed `.py` files | **clean** (12 files reformatted in commit `984a3e8`) |
| `pytest tests/` | full backend | **887 passed, 35 skipped** (was 859 baseline → +28 net new) |
| `npm run type-check` | full frontend | **0 errors** |
| `npm run test:unit` | full frontend | **299 passed** (was 237 baseline → +62 net new, including 1 regression test added in this verify pass) |
| `npm run lint` | full frontend | 24 errors, **0 net new** (all pre-existing) |

Bundle size after `npm run build`: woff2 fonts 248 KB total / `dist/assets/index-*.js` ≈ 133 KB / `index-*.css` ≈ 39 KB. Under NFR-05 600 KB cap.

Pre-existing baseline lint debt (UP042 enum-str pattern in `models.py`, N814 import alias) is intentional and out of scope for this branch.

---

## Phase 3 — Multi-Agent Code Review

Two parallel reviewer subagents:

**A. CLAUDE.md compliance** — no 75+ confidence violations. Two informational items (55-60 confidence) noted: (1) `BEGIN IMMEDIATE` raw-SQL inside SQLAlchemy auto-begin transaction is unusual but tested and works; (2) function-local `from datetime import` in `processing.py` could be hoisted. Neither blocks merge.

**B. Bug scan + cross-file consistency** — one 75+ confidence issue surfaced and **fixed in this pass**:

| # | Finding | Confidence | Resolution |
|---|--------|-----------|-----------|
| **4** | `reader.ts:139-156` — every `navigateSection` triggered two `getSection()` round-trips: once via `BookDetailView`'s route watcher and once via the explicit `await loadSection(...)` after `router.push`. | **75** | Fixed in `721cfb1` — added in-flight promise dedupe keyed on `(bookId, sectionId)`. Regression test added (`reader.tab.spec.ts:144`). |
| 1 | BEGIN IMMEDIATE atomicity on TOCTOU stale-recover path | 70 | Logged as follow-up. Behavior is correct in tested paths; the corner case is "stale-mark + INSERT-IntegrityError" which is rare and currently rolls back both writes (acceptable: the next POST will redo the sweep). |
| 2 | 409 body shape inconsistent with sibling routes | 65 | Logged as follow-up. The new shape is consumed by F10 deep-link UI; sibling routes (`book_summary.py`, `sections.py`) still emit string detail — harmonization deferred to a future polish PR. |
| 5 | `useBufferedJobStream` reconnect race against buffered events | 55 | Logged as follow-up. F10 fix (`0ccb4ce`) already normalizes status; reconnect-error path is best-effort by design (see plan Decision P9). |
| 6 | Buffer leak on degraded networks (uncapped retry buffer) | 60 | Logged as follow-up. Personal-scale tool; bounded buffer is a future hardening. |
| 7 | `column_property(deferred=False)` adds `LENGTH(content_md)` to all BookSection selects | 55 | Logged as follow-up. Per CLAUDE.md gotcha 1, eager loading is the project convention; the plan's Decision P4 explicitly chose this over a stored column. SQLite `LENGTH()` is O(content size); routes that don't need char count could later switch to `deferred=True` + `undefer()`. |

---

## Phase 4 — Deploy & Integration (cited evidence from TN run)

The TN subagent's pass on the same branch tip provided fresh runtime evidence. Re-run only if changes since then would invalidate it; this verify pass touched only `reader.ts` (frontend store) and ruff cosmetics (no behavior change), so TN's evidence remains valid.

| Step | Evidence (from TN, see plan TN section §2945+) |
|------|------------------------------------------------|
| 3a. Migrations on fresh DB | Chain ends at `9a67312a27a7` (preset typography) → `e0c48efb7afe` (legacy image_id rewrite). `alembic upgrade head` clean. |
| 3b. Server | Verification server on :8766 (8765 was busy on host); `/api/v1/health` returned `{"status":"ok","llm_provider":"claude","llm_available":true}`. |
| 3c. API smoke | `/books/2` includes `sections[].content_char_count` (B7 ✓). `/processing/999999` → 404. `/processing/6` returns spec §7.4 shape (B6 ✓). `POST /books/1/summarize` → first call 200, second call 409 with structured `detail.active_job` payload (B2 ✓). |
| 3d. Frontend (Playwright MCP) | F2 (theme switching paints distinct palettes), F6 (ThemeGrid renders previewFont), F7 (inactive tab is muted-clickable, not disabled), F3 (SectionListTable 6 cols full / compact in TOC), F5 (split-button default download + chevron Copy menu), F8 (`?tab=summary` survives navigation + back-button), F10 (`/jobs/:id` paints PENDING/RUNNING/COMPLETED/FAILED + 404 from cold reload). 0 console errors. |
| 3e. Edge cases | F10 lowercase status normalization caught + fixed (`0ccb4ce`); regression test added in `useBufferedJobStream.spec.ts`. |

---

## Phase 5 — Compliance Tables

### 5a. Spec §12.6 Bug Battery (8 confirmed bugs)

| # | Bug | Outcome | Evidence |
|---|-----|---------|----------|
| 1 | Stale job blocks summarize retry | Verified | B1+B2 commits + `test_processing_stale_guard.py` + TN curl `POST /books/1/summarize` flow |
| 5 | Inactive content tab styled as disabled | Verified | F7 commit `f34a03e` + TN Playwright DOM assertion (`opacity:1`, `cursor:pointer`, `disabled:false`) |
| 6 | Broken images in summaries (`image:N`) | Verified | B3+B4+B5 commits + `test_image_url_rewrite_id_scheme.py` + `test_migration_image_id_rewrite.py` + migration applied cleanly on fresh DB |
| 7 | Tab choice not surviving reload | Verified | F8 commit `d47cb2c` + `reader.tab.spec.ts` (6 tests) + TN Playwright back-button assertion |
| 8 | Action-row hierarchy reads accidental | Verified | F9a commit `d4d7a7e` + TN ARIA snapshot (`aria-haspopup="menu"` on chevron + "More actions" overflow) |
| 9 | Copy-to-clipboard failure | Verified | F5+F9b commits + clipboard text-only fallback path + B5 migration also fixes underlying image refs |
| 10 | Theme presets identical / partially no-op | Verified | F2 commit `e0d9f9b` + TN computed-color assertion (Light `#fff` → Night `#0a0e1a` → Paper `#f7f1e3` → Contrast `#000`) |
| 11 | Duplicate progress counter | Verified | F9a removed static counter; SummarizationProgress is sole source |

### 5b. Spec FR-ID compliance (46 IDs)

Grouped by track. Outcome semantics: **V** = Verified with cited evidence; **NA** = no runtime surface (covered by alternative); **U** = unverified blocker.

| FR group | IDs | Outcome | Evidence |
|----------|-----|---------|----------|
| Stale-job sweep | FR-01, FR-02, FR-03, FR-04 | V | `test_processing_stale_guard.py` + `test_orphan_sweep_predicate.py` + TN `POST /summarize` curl |
| Image rewrite | FR-05, FR-06, FR-07, FR-08, FR-09 | V | 7 unit tests in `test_image_url_rewrite_id_scheme.py` + B5 migration test + summarizer wiring at 3 call sites + prompt grep clean |
| Job deep-link | FR-10, FR-11, FR-11a, FR-11b, FR-12, FR-12a | V | B6 `test_processing_get_endpoint.py` (5 cases) + F10 `useBufferedJobStream.spec.ts` (10 tests, including reconnect-on-error and lowercase-status normalization) + TN Playwright `/jobs/6`, `/jobs/7`, `/jobs/999999` |
| Reader tab URL | FR-13, FR-14, FR-15, FR-16, FR-17 | V | F8 `reader.tab.spec.ts` (6 tests, including the regression test added in this verify pass) + TN back-button walk |
| Action row redesign | FR-18, FR-19, FR-20, FR-21, FR-22, FR-22a, FR-23 | V | F9a `BookOverviewView.spec.ts` + TN action-row screenshot |
| Section list | FR-24, FR-25, FR-26, FR-27, FR-28, FR-29, FR-30, FR-31, FR-32, FR-33, FR-33a | V | B7 `test_book_detail_char_count.py` + F3 `SectionListTable.spec.ts` (10) + `TOCDropdown.spec.ts` (2) + TN 6-col table screenshot |
| Theme + typography | FR-34, FR-35, FR-36, FR-37, FR-38, FR-39, FR-39a | V | F2 theme.css 17-var blocks + B8 preset migration + F6 ThemeGrid preview test + TN computed-color check |
| Webfont bundle | FR-40 | V | F1 `@fontsource` imports in `main.ts` + bundle 248 KB ≤ 600 KB cap |

**No FR-ID has Outcome = U.** All 46 resolve to V or are covered by an enumerated test/screenshot.

### 5c. Plan task compliance (19 tasks)

| Task | Outcome | Commit | Evidence |
|------|---------|--------|----------|
| B1 | V | `6581d6f` | `test_orphan_sweep_predicate.py` (5 tests) |
| B2 | V | `006555c` | `test_processing_stale_guard.py` (2 tests) + concurrent test rewrite |
| B3 | V | `27a1b8f` | `test_image_url_rewrite_id_scheme.py` (7 tests) |
| B4 | V | `f2fde8d` | summarizer wiring at 3 sites + prompt grep clean |
| B5 | V | `9900125` | `test_migration_image_id_rewrite.py` (3 tests) + fresh-DB upgrade clean |
| B6 | V | `f64fe94` | `test_processing_get_endpoint.py` (6 tests) |
| B7 | V | `fc266f4` | `test_book_detail_char_count.py` (2 tests) |
| B8 | V | `93bbe6c` | `test_migration_preset_typography.py` (3 tests) |
| F1 | V | `8c786b6` | bundle 248 KB ≤ 600 KB |
| F2 | V | `e0d9f9b` | 6 themes × 20 vars (verified by parity grep) |
| F3 | V | `99058f6` | `SectionListTable.spec.ts` (10) + `TOCDropdown.spec.ts` (2) |
| F4 | V | `bf86470` | `FontListbox.spec.ts` (6) + `CustomEditor.spec.ts` (1) |
| F5 | V | `c6d3276` | `ExportSplitButton.spec.ts` (7) + `OverflowMenu.spec.ts` (5) |
| F6 | V | `ee8c439` | `ThemeGrid.spec.ts` typography preview (1 added) |
| F7 | V | `f34a03e` | `ContentToggle.spec.ts` (2 new tests) |
| F8 | V | `d47cb2c` | `reader.tab.spec.ts` (5 + 1 regression added in verify) |
| F9a | V | `d4d7a7e` | `BookOverviewView.spec.ts` (5 new assertions) + 2 export.spec retargeted |
| F9b | V | `05f2f33` | `BookOverviewView.export.spec.ts` (3 new assertions for 250 ms floor + clipboard fallback) |
| F10 | V | `56909b9` + `0ccb4ce` | `useBufferedJobStream.spec.ts` (10 + 2 regression) + `JobProgressView.spec.ts` (5) |

### 5d. Gap report

**No critical gaps.** Six informational follow-ups logged in Phase 3 review (BEGIN IMMEDIATE atomicity edge case, 409 shape harmonization, useBufferedJobStream reconnect race, buffer cap on degraded networks, column_property eagerness, function-local imports). All scored 50-70 confidence and do not block this branch.

---

## Phase 6 — Test Suite Hardening

| Issue | Where | Test added |
|-------|-------|-----------|
| F10 lowercase status normalization | `useBufferedJobStream.ts` | 2 tests in `useBufferedJobStream.spec.ts` (lowercase normalization + null-progress tolerance) — added in TN run, commit `0ccb4ce` |
| Double-fetch on `navigateSection` | `reader.ts` | 1 test in `reader.tab.spec.ts:144` (`loadSection dedupes concurrent calls for the same (book, section)`) — added in this verify pass, commit `721cfb1` |

---

## Phase 7 — Final Compliance Pass

- No `TODO/FIXME/HACK` left in changed files (`grep` clean).
- No debug logging or temp code.
- No hardcoded values that should be config (preset typography is via Alembic migration; theme palette is in CSS vars).
- CLAUDE.md not updated — none of the gotchas/conventions are new. The new GET endpoint + column_property follow existing patterns.
- No changelog entry yet — call `/changelog` after merge to main per CLAUDE.md "Workflows" guidance.

---

## Phase 8 — Commit & Report

This pass produced 3 commits:
- `0ccb4ce` `fix(jobs): normalize lowercase status from API in /jobs/:id deep-link` (caught by TN, fixed during TN)
- `984a3e8` `chore(verify): ruff format + auto-fix scoped to changed files`
- `721cfb1` `fix(reader): dedupe in-flight loadSection to avoid double-fetch on nav` (caught by Phase 3 multi-agent review)

Branch tip: `721cfb1`. Not pushed, not merged. Recommended next step: `git push -u origin feature/reader-bookdetail-ux-fixes && gh pr create` to open the PR. After merge, run `/changelog` to record user-facing changes.

---

## Open follow-ups (not blockers)

1. Consider harmonizing 409 body shape across `book_summary.py` / `sections.py` / `processing.py` (review issue #2).
2. Cap `useBufferedJobStream` reconnect buffer at N events (review issue #6).
3. `BookSection.content_char_count` is `deferred=False` — consider `deferred=True` + `undefer()` only at API serialization sites if profiling shows hot routes (review issue #7).
4. Consider hoisting the `from datetime import datetime, timedelta` inside `processing.py:start_processing` to module top (CLAUDE.md compliance, informational).

These were logged as 50-74 confidence by the multi-agent review and do not block merge.
