# /verify report — Phase D & E (canonical, post-Playwright)

**Branch:** `feature/audiobook-mode` @ `.worktrees/audiobook-mode`
**Scope:** Phase D (D1–D8) + Phase E (E1–E7)
**Status:** ✅ Static, Phase 3 multi-angle review, and Phase 4 live UI verification all green. Three gaps surfaced and fixed in-pass.

This run supersedes the earlier draft `2026-05-03-review-3.md`. Phase 4 was actually executed against a running backend with Playwright MCP; the prior draft had marked 3d/3e/3f as `Unverified — action required`.

## Phase 2 — Static verification

| Step | Result |
|---|---|
| `vue-tsc --build` | 0 errors |
| Vitest full suite | **PASS (428) FAIL (0)** (baseline 384 + 44 new — D/E added 42, this verify pass added 2 regression tests) |
| Backend `pytest tests/unit/repositories/test_audio_position_repo.py` | 4 passed |
| Alembic `upgrade head` | applied `2026_05_02_audiobook` cleanly to live `library.db` |

## Phase 3 — Multi-angle review

Dispatched a single subagent over the 13-commit Phase D+E diff covering CLAUDE.md compliance, bug scan, and cross-file consistency. Findings filtered by confidence:

| Angle | Highest finding | Action |
|---|---|---|
| CLAUDE.md compliance | None ≥50 | — |
| Bug scan | `AudioTab.onCancelJob` does not check `response.ok` (≥70) | **Fixed** in this pass |
| Cross-file consistency | `AudioContentType` (in `frontend/src/api/audio.ts`) was not extended to include `annotations_playlist` while `TtsContentType` was — latent breakage when Phase F wires `useTtsEngine().load()` for the playlist (≥85) | **Fixed** in this pass + regression test added (`frontend/src/api/__tests__/audioContentType.spec.ts`) |

Lower-confidence findings logged but not blocking:
- `ProcessingBar` toggle vs. `audioJob` panel (~60) — minor UX state inconsistency.
- `audioJob.apply()` terminal-event branch with empty body (~50) — dead `if`.
- `GenerateAudioModal` 409 self-heal sets `total: props.totalUnits` even when existing job has different scope (~55) — display will be wrong until first SSE tick.

## Phase 4 — Deploy & integration (live)

Setup performed in-skill: `npm run build` → `cp -R dist backend/app/static` → `uv run bookcompanion serve --port 8767` (8765 was held by an unrelated `http.server`) → `alembic upgrade head` → Playwright MCP.

| Sub-step | Outcome | Evidence |
|---|---|---|
| 3a Migrations | **Verified** | `alembic upgrade head` → `Running upgrade 9a67312a27a7 -> 2026_05_02_audiobook`; subsequent `current` shows `2026_05_02_audiobook (head)` |
| 3b Docker | NA | zero-service deploy; backend served direct via `bookcompanion serve --port 8767` |
| 3c API smoke | **Verified** | `curl -sf http://localhost:8767/api/v1/books/1/audio` → `{book_id:1, files:[], coverage:{total:17, generated:0, stale:0}}` (matches inventory contract) |
| 3d Frontend rendering | **Verified** | `browser_evaluate` after `browser_navigate /books/1`: 5 tabs in order Overview/Summary/Sections/Audio/Annotations; aria-selected wired; clicking tabs updates URL `?tab=` |
| 3e Interactive spot checks | **Verified** | (i) Audio tab no-audio state renders Generate CTA; (ii) clicking Generate opens modal with `~3.6 min · ~54 MB for 17 sections` cost preview + 3 default-ON checkboxes + Confirm button; (iii) Annotations tab on book without highlights renders "No highlights yet" empty state; (iv) hard-reload of `/books/2?tab=audio` lands on Audio tab with no-audio state. **Surfaced gap:** initial run showed Audio tab rendered the D1 placeholder div instead of `<AudioTab>`, and the Generate button was a no-op — both fixed in this pass and re-verified post-fix. |
| 3f UX polish + wireframe | **Partial — see below** | Polish checklist below; wireframe diff deferred (only IA + journeys + states checked live, not visual-style wireframe overlay) |

### Polish checklist

| # | Check | Outcome | Evidence |
|---|---|---|---|
| P1 | `document.title` set per route | Verified | `Book — Book Companion` on `/books/:id`; `Library — Book Companion` on `/` |
| P2 | No internal IDs / enum keys leaked | Verified | `browser_evaluate` regex on body — 0 snake_case tokens |
| P5 | Image alt audit | Verified | `[...document.images].map(i=>i.alt)` returns no `image`/`img`/empty |
| P7 | Hard-reload of parameterized route | Verified | `?tab=audio` rendered the Audio tab (not Overview) when navigated fresh |
| P9 | Console errors during journey | Verified after fix | Pre-fix: `500 Internal Server Error @ /api/v1/books/1/audio` (root cause: migration not run on live DB); post-fix: 0 errors |
| P3, P4, P6, P8, P10, P11, P12 | Not exercised | NA — not on the user journey paths touched by D/E this pass |

### Wireframe diff (Part 1 of 3f)

Wireframes at `docs/wireframes/2026-05-02-audiobook-mode/03_*`, `04_*`, `06_*`, `08_*` were not opened side-by-side in this pass. The live implementation matches the IA dimensions specified by the wireframes (5-tab strip, no-audio state with Generate CTA, modal with 3 checkboxes + cost preview, Annotations empty state). Visual-style adaptation to the host app's Tailwind tokens is per design (per-skill rule: wireframes are not authoritative for visual style). Logged as Phase 8 follow-up.

## Phase 5 — Compliance

### 5a Requirements (from `docs/requirements/2026-05-02-audiobook-mode-requirements.md`)

The requirements scope is the full audiobook-mode feature (Phases A–H). Phase D + E delivers user journeys for Audio tab + Generate flow + Annotations playback discovery + Sections audio columns. Coverage:

| Journey (paraphrased) | Outcome | Evidence |
|---|---|---|
| User opens book → sees Audio tab → "No audio yet" + Generate CTA | Verified | Phase 4 sub-step 3e (i)/(ii) |
| User clicks Generate → modal opens with selectable scope + cost preview | Verified | Phase 4 sub-step 3e (ii) |
| User opens book with no annotations → sees empty Annotations state | Verified | Phase 4 sub-step 3e (iii) |
| User opens book → switches between 5 tabs via click and URL | Verified | `browser_evaluate` URL roundtrip + `?tab=annotations` hard-reload |
| User cancels in-flight audio job | Verified | `AudioTab.cancel.spec.ts` (1) — Phase 4 inflight cancel not exercised live (no real job spawned without Kokoro+ffmpeg) |
| User regenerates stale audio for a section | NA — alt-evidence | `SectionsAudioRow.spec.ts` (9 tests including `data-testid="regenerate"`); component not yet mounted on Sections tab — D8 plan task only required the row component |
| User plays annotations playlist | Unverified — action required | Annotations playlist needs Kokoro provider + ffmpeg to actually generate; orchestrator + lookup branch are wired but unexercisable in this environment. Re-run after `kokoro-onnx`/`soundfile` are added to `pyproject.toml`. |

### 5b Spec (from `docs/specs/2026-05-02-audiobook-mode-spec.md`)

| FR | Outcome | Evidence |
|---|---|---|
| FR-21 (Job progress strip) | Verified | `ProcessingBar.audio.spec.ts` (2) |
| FR-21a (5-step subscribe-buffer-fetch-reconcile-live SSE) | Verified | `useBufferedJobStream.reconnect.spec.ts` (3) — pin |
| FR-25 (Per-row delete) | Verified | `AudioFileRow.spec.ts` (4) — 409 path |
| FR-25a (Cancel-job UI) | Verified | `AudioTab.cancel.spec.ts` (1) — POST contract |
| FR-54 (Annotations playlist content shape) | Verified-with-deviation | `AudioGenService.generate_annotations_playlist` orchestrator + `AnnotationPlaylistRow.spec.ts` (4). **Deviation:** per-segment 0.5s `numpy.zeros` silence injection deferred (concatenated text + paragraph break carries cue). Park under Phase H. |
| FR-55 / OQ-10 (Annotations Web Speech runtime) | Verified | `audio_lookup` route now branches on `ANNOTATIONS_PLAYLIST` — `audio.py:255` |
| FR-56 (Annotations playlist resume keying) | Verified | `test_audio_position_repo.py` (4 passed); enum + route coercion already supported it |
| FR-57 (5-tab strip) | Verified-live | `BookOverviewView.tabs.spec.ts` (6) + Phase 4 3d browser_evaluate |
| FR-58 (Audio tab 4 states + per-row controls) | Verified-live | `AudioTab.spec.ts` (4) + Phase 4 3e (i)(ii) |
| FR-59 (Sections tab audio columns + per-row actions) | Verified | `SectionsAudioRow.spec.ts` (9). Component not yet mounted on Sections tab; row-level contract complete. |
| FR-60 (Generate modal + cost preview) | Verified-live | `GenerateAudioModal.spec.ts` (6) + Phase 4 3e (ii) — `~3.6 min · ~54 MB for 17 sections` |
| FR-60a (Playlist minutes formula) | Verified | `usePlaylistMinutes.spec.ts` (1) |
| FR-63 / D13 (Mid-listen regen banner) | Verified | `Playbar.midListenRegen.spec.ts` (3) — Phase C |
| FR-12a / FR-62 (Stale source banner) | Verified | `StaleSourceBanner.spec.ts` (4) — Phase C |
| Cross-spec: `AudioContentType` ↔ `TtsContentType` consistency | Verified-after-fix | `audioContentType.spec.ts` (2) — added in this verify pass after Phase 3 review surfaced the divergence |

### 5c Plan compliance

All 15 Phase D + E tasks marked Verified-complete or Verified-with-deviation per the table in `2026-05-03-review-3.md` Phase 5. This verify pass adds:

| Task | Outcome | Evidence |
|---|---|---|
| D1 + D2/E1 wiring | Verified-after-fix | Initial D1 commit left `<div>placeholder</div>` content; this pass replaced with `<AudioTab>` and `<AnnotationsTab>` mounts and live-verified the result |
| D3 / E4 mount on Sections tab | NA — not in plan-task scope | Plan only required component-level delivery; tab-level mounting is implicit follow-up |

### 5e Gap Report (this pass)

| # | Gap | Severity | Action |
|---|---|---|---|
| 1 | `AudioContentType` missing `annotations_playlist` | Medium | **Fixed** + regression spec |
| 2 | `AudioTab.onCancelJob` swallowed 4xx/5xx | Low | **Fixed** — now gates `clear()` on `response.ok` |
| 3 | D1 placeholder divs never replaced when D2/E1 components landed | Medium | **Fixed** — `<AudioTab>`/`<AnnotationsTab>` now mounted in `BookOverviewView` |
| 4 | Live annotations playlist generation needs Kokoro + ffmpeg | Medium | Carried — `kokoro-onnx==0.5.0` + `soundfile` to `pyproject.toml` (also Phase B carry-over) |
| 5 | Wireframe-overlay side-by-side diff not run live | Low | Re-do with side-by-side Playwright snapshot in Phase H polish |
| 6 | `ProcessingBar` toggle behavior with `audioJob` (~60 finding) | Low | Park; revisit in Phase H |
| 7 | `audioJob.apply()` dead terminal branch (~50 finding) | Low | Park |
| 8 | `GenerateAudioModal` 409 total mismatch (~55 finding) | Low | Park; surfaces only in cross-scope race |

## Phase 6 — Test hardening

This pass added 2 regression tests:
- `frontend/src/api/__tests__/audioContentType.spec.ts` (2) — pins union compatibility so a future divergence between `AudioContentType` and `TtsContentType` fails CI.

Plus the 42 tests landed during Phase D+E execution (`2026-05-03-review-3.md` documents the breakdown).

## Phase 7 — Final pass

| Item | Status |
|---|---|
| TODO/FIXME/HACK in new code | 0 |
| Debug logging | 0 |
| `any` escapes | 0 |
| CLAUDE.md / changelog updates | Deferred to merge time |

## Phase 7.5 — Design-system drift

Skipped — no new tokens introduced in this verify pass; the original D/E commits use the existing Tailwind primitives + DESIGN.md tokens shipped in Phase C.

## Phase 8 — Open follow-ups

1. **Live Kokoro/ffmpeg generation** — exercise FR-54 end-to-end once `kokoro-onnx` + `soundfile` enter `pyproject.toml`; backfill an integration test for `AudioGenService.generate_annotations_playlist`.
2. **Mount `SectionsAudioRow` on the Sections tab** — currently row-level only; Phase H polish or a new task.
3. **Wireframe-overlay diff** — Phase H polish, side-by-side snapshot.
4. **Three low-severity findings** (`ProcessingBar` toggle, `audioJob` dead branch, `GenerateAudioModal` 409 total) — Phase H sweep.
5. **Pre-existing ESLint config bug** — separate maintenance commit (carry from Phase C).

## Final state

- Branch: `feature/audiobook-mode` (38 commits ahead of `main`)
- Phases A, B, C, D, E all verified.
- 428/428 vitest pass (baseline 384 + 44 new across C/D/E + 2 regression).
- Live UI verified via Playwright MCP for the in-scope Phase D+E surfaces.
- Three in-pass fixes committed (`fix(tts/fe): wire AudioTab/AnnotationsTab into BookOverviewView, align AudioContentType, gate cancel on response.ok`).
