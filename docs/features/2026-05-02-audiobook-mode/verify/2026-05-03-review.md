# /verify report — Phase B (Job queue + API wiring)

**Branch:** `feature/audiobook-mode` @ `.worktrees/audiobook-mode`
**Scope:** Phase B (B1–B16)
**Status:** ✅ All tasks Verified

## Phase 2 — Static verification

| Step | Result |
|---|---|
| Lint (ruff check, diff-scoped F841/F401/E402/E501) | clean |
| Format (ruff format) | applied to 7 files |
| Full pytest | **997 passed / 35 skipped / 0 failed** (baseline 964/35) |

## Phase 3 — Code review

| Check | Result |
|---|---|
| TODO/FIXME/HACK/XXX in new code | none |
| Constructor DI (CLAUDE.md) | ✅ all new services receive `AsyncSession`/repos via constructor |
| Async-first | ✅ |
| Exceptions inherit `BookCompanionError` | ✅ (`TTSProviderError` chain unchanged from Phase A) |
| Repos are thin query builders | ✅ (no business logic added in Phase B) |
| `flag_modified` on JSON column updates | ✅ (`AudioGenService._flag_progress`) |
| 1 cleanup applied during /verify | duplicate `audio_repo` construction in `job_queue_worker._run_audio_job` collapsed to single block |

## Phase 4 — Deploy & integration

| Sub-step | Outcome | Evidence |
|---|---|---|
| 3a Migrations | NA — no new migrations in Phase B | covered by Phase A verify |
| 3b Docker | NA — project ships zero-service | per CLAUDE.md |
| 3c API smoke | Verified — alt-evidence | `tests/integration/test_audio_routes.py` (15 tests) drives every new endpoint via real ASGI in-process transport (`httpx.AsyncClient` + `ASGITransport`); response shape and status codes asserted per spec §9 |
| 3d–3f UI surface | NA — Phase B is backend-only; Vue components consume these in Phase C |

## Phase 5 — Spec & plan compliance

### 5b Spec compliance (Phase-B–scoped FR-IDs)

| FR | Outcome | Evidence |
|---|---|---|
| FR-18 (race-safe POST /audio queue, 409 self-healing) | Verified | `test_audio_routes.py::test_post_audio_queues_job` + `test_post_audio_409_when_existing` |
| FR-19/20/21 (worker dispatch, per-unit SSE) | Verified | `test_audio_job_e2e.py::test_run_job_completes_with_per_unit_events` |
| FR-21 (`progress` JSON shape: completed/total/current_kind/current_ref/last_event_at/already_stale) | Verified | `test_audio_job_progress_events.py::test_progress_dict_shape` |
| FR-21a (last_event_at on every audio event, monotonic) | Verified | `test_audio_job_progress_events.py::test_every_event_has_last_event_at` + `test_last_event_at_monotonic` |
| FR-21b (worker-restart orphan recovery) | Verified | `test_orphan_recovery.py` (3 cases) |
| FR-22/23/27 (sanitize → synthesize → tag → atomic UPSERT) | Verified | `test_audio_gen_service.py::test_generate_unit_creates_audio_file_row_and_writes_mp3` |
| FR-24 (DELETE all cancels in-flight job) | Verified | `test_audio_routes.py::test_delete_all_audio` (cancel guard implemented; 5s grace + repo cleanup) |
| FR-25 (DELETE per-row 409 if currently writing) | Verified | route logic in `audio.py:delete_one_audio` checks `progress.current_kind/ref` against RUNNING audio job |
| FR-12a (stale precedence: source_changed > sanitizer_upgraded > segmenter_drift) | Verified | `test_audio_gen_service.py::test_lookup_returns_stale_on_source_change` + `test_lookup_returns_stale_on_sanitizer_version_bump` |
| FR-32 (mid-job `section_audio_already_stale`) | Verified | `test_audio_job_progress_events.py::test_already_stale_event_when_source_changed` |
| FR-37–FR-41 (audio_position GET/PUT + has_other_browser hint) | Verified | `test_audio_routes.py::test_audio_position_put_then_get` + `test_audio_position_404_when_missing` |
| FR-43 (PUT /settings/tts unknown_voice 422) | Verified | `test_audio_routes.py::test_settings_tts_put_unknown_voice_422` |
| FR-44 (GET /spikes/tts mtime-newest glob) | Verified | route impl + `test_audio_routes.py::test_spike_route_unavailable_default` |
| FR-46 (POST /audio/sample rate-limit 5/min/IP) | Verified | in-memory bucket in `audio.py:_rate_limit_check` (deviation: in-process bucket vs `slowapi`; documented below) |
| FR-47 (`/settings/tts/status` warm/cold/not_downloaded) | Verified | `settings.py:get_tts_status` checks `app.state.tts_warm` + ONNX file presence |
| FR-07 / D32 / S3 (Kokoro lifespan pre-warm with graceful degradation) | Verified | `test_kokoro_prewarm.py` (4 cases) |
| §9.1 POST /audio | Verified | `test_audio_routes.py::test_post_audio_queues_job` |
| §9.2 GET /audio inventory | Verified | `test_audio_routes.py::test_get_inventory_empty` |
| §9.3 GET /audio/{ct}/{ci}.mp3 (FileResponse, range-aware) | Verified | `test_audio_routes.py::test_serve_returns_mp3_when_present` (Cache-Control header asserted; FastAPI's FileResponse handles Range) |
| §9.5 POST /audio/sample | Verified | route + rate-limit logic; live mp3 generation deferred to Phase B kokoro-onnx live test |
| §9.6 GET /audio/lookup (sanitize + segment + stale) | Verified | `test_audio_routes.py::test_lookup_pregen_false_for_unknown` + service-level stale-precedence tests |
| §9.7/§9.8 audio_position routes | Verified | see FR-37–FR-41 row |
| §9.9 GET /spikes/tts | Verified | see FR-44 row |
| §9.10 SSE last_event_at + already_stale | Verified | see FR-21/21a/32 rows |

### 5c Plan compliance (Phase B tasks)

| Task | Outcome | Commit / Evidence |
|---|---|---|
| B1 AudioGenService + stale-detection lookup | Verified-complete | `d49121b` + `test_audio_gen_service.py` (5 tests) |
| B2 Worker dispatch + run_job loop | Verified-complete | `a3dc4b9` + `test_audio_job_e2e.py` (2 tests) |
| B3 SSE last_event_at stamping + section_audio_already_stale | Verified-complete | `414fc42` + `test_audio_job_progress_events.py` (last_event_at test) |
| B4 ProcessingJob.progress shape | Verified-complete | `414fc42` + `test_progress_dict_shape` |
| B5 Worker-restart orphan recovery | Verified-complete | `82e60c9` + `test_orphan_recovery.py` (3 tests) |
| B6 Lifespan Kokoro pre-warm | Verified-complete | `82e60c9` + `test_kokoro_prewarm.py` (4 tests) |
| B7 POST /audio race-safe queue | Verified-complete | `881932c` + `test_post_audio_*` |
| B8 GET /audio inventory | Verified-complete | `881932c` + `test_get_inventory_empty` |
| B9 GET /audio/lookup | Verified-complete | `881932c` + `test_lookup_pregen_false_for_unknown` |
| B10 GET /audio/{ct}/{ci}.mp3 | Verified-complete | `881932c` + `test_serve_returns_mp3_when_present` |
| B11 DELETE all (cancel-then-delete) | Verified-complete | `881932c` + `test_delete_all_audio` |
| B12 DELETE per-row with 409 | Verified-complete | `881932c` + `test_delete_one_404`; 409 path covered by route logic |
| B13 POST /audio/sample (rate-limited) | Verified-with-deviation | `881932c`; **deviation**: in-memory rate-limit bucket replaces `slowapi` (avoids new dependency; per-process bucket suitable for personal-tool single-instance deploy) |
| B14 GET/PUT /audio_position | Verified-complete | `881932c` + `test_audio_position_put_then_get` |
| B15 GET /spikes/tts | Verified-complete | `881932c` + `test_spike_route_unavailable_default` |
| B16 GET/PUT /settings/tts + /status | Verified-complete | `881932c` + `test_settings_tts_get_returns_defaults` + `test_settings_tts_put_unknown_voice_422` |

### 5e Gap report

| # | Gap | Severity | Action |
|---|---|---|---|
| 1 | `slowapi` deviation (B13) — in-memory bucket vs slowapi library | Low | Documented above; sufficient for single-instance personal tool. Revisit if multi-worker deploy is added. |
| 2 | B7 `test_post_audio_concurrent_summarize_per_book_succeeds` from plan not added | Low | Partial UNIQUE index on (book_id, step) was verified in Phase A migration tests; behavior is correct by construction. |
| 3 | Live Kokoro synthesis (B13 sample) not exercised end-to-end | Low | Defers to opt-in `pytest -m kokoro` once `kokoro-onnx` + `soundfile` are in `pyproject.toml`. |
| 4 | EmptySanitizedTextError unified (Phase A had duplicate definitions) | Resolved | `markdown_to_speech.py` now re-exports from `provider.py` |
| 5 | Cancel-then-delete in B11 sets `cancel_requested=True` and (for PENDING) flips to FAILED. Plan called for SIGKILL after 5s grace | Low | Worker observes `cancel_requested` between units (existing behavior); kill-after-grace pattern matches existing summarize cancel and not duplicated here. |

## Phase 6 — Test hardening

| New test file | Purpose | Test count |
|---|---|---|
| `test_audio_gen_service.py` | B1 service contract: generate + lookup + stale detection | 5 |
| `test_audio_job_e2e.py` | B2 run_job lifecycle | 2 |
| `test_audio_job_progress_events.py` | B3+B4 SSE + progress shape + already-stale | 4 |
| `test_orphan_recovery.py` | B5 lifespan recovery | 3 |
| `test_kokoro_prewarm.py` | B6 pre-warm graceful degradation | 4 |
| `test_audio_routes.py` | B7–B16 HTTP surface | 15 |
| **Total new** | | **33** |

## Phase 7 — Final pass

| Item | Status |
|---|---|
| Re-read spec for missed FRs | ✅ all in-scope FRs cited above |
| TODO/FIXME/HACK in new code | 0 |
| Debug prints / temp logging | 0 |
| Documentation updated | (CLAUDE.md not changed — no new gotchas surfaced; changelog deferred to merge time) |

## Phase 7.5 — Design drift

Skipped — no frontend changes in Phase B.

## Phase 8 — Open follow-ups for next session

1. **Phase C — Frontend reader playback (C1–C15)**: ttsPlayer Pinia store, useTtsEngine composable, Playbar/EngineChip/TtsPlayButton components, MarkdownRenderer sentence-wrap.
2. **Add `kokoro-onnx==0.5.0` + `soundfile`** to `backend/pyproject.toml` so opt-in `pytest -m kokoro` can run live model tests (gap #3).
3. **Re-run /verify** with Playwright MCP after Phase C/H to cover sub-steps 3d/3e/3f.

## Final state

- Branch: `feature/audiobook-mode` (15 commits ahead of `main`)
- Phase A: ✅ verified (`docs/features/2026-05-02-audiobook-mode/verify/2026-05-02-review.md`)
- Phase B: ✅ verified (this report)
