# TN — Final Verification (Phases F + G + H)

**Date:** 2026-05-03
**Branch:** audiobook-mode (40+ commits ahead of main)
**Scope:** Phases F (Settings TTS + spike CLI), G (CLI listen/maintenance), H (Media Session + size cap + stale banner).

## Static gates

| Check | Result |
|---|---|
| `npm run type-check` | 0 errors |
| `npm run test:unit` | **440 passed / 0 failed** |
| `npm run build` | dist generated; chunk warning unrelated to this feature |
| `cd backend && uv run ruff check app/cli/commands/{listen,spike,maintenance}_cmd.py` | 0 errors |
| Backend pytest (unit + integration, `not kokoro and not integration_llm`) | **993 passed / 35 skipped / 4 deselected** |
| Pre-existing ESLint config bug | unchanged; not introduced by this work |

## Live API smokes (port 8765)

| Endpoint | Result |
|---|---|
| `GET /api/v1/health` | `{"status":"ok","llm_provider":"claude",...}` |
| `GET /api/v1/settings/tts` | `{engine:"web-speech", voice:"", default_speed:1.0, auto_advance:true, prewarm_on_startup:true}` |
| `GET /api/v1/spikes/tts` | `{"available":false}` |
| `GET /api/v1/audio/stale-books` | `{"books":[]}` (empty — no audio yet) |
| `GET /api/v1/books/1/audio` | `{coverage:{total:17, generated:0, stale:0}}` |
| `GET /api/v1/audio/lookup?book_id=1&content_type=section_summary&content_id=1` | `pregenerated:false`, sanitized text returned |

## Live UI smokes (Playwright MCP)

| Route | Verified |
|---|---|
| `/settings/tts` | `[data-testid="settings-tts-panel"]` mounted; both engine radios; SpikeFindingsBlock renders "Spike not yet run" copy + CLI hint; KokoroStatusIndicator renders status pill; Save button present |
| `/` | RegenerateStaleAudioBanner correctly hidden (0 stale books); BookGrid mounts |
| `/books/1?tab=audio` | AudioTab mounts; no-audio state with Generate CTA |
| Console errors across all routes | **0** |

## CLI smokes

| Command | Result |
|---|---|
| `bookcompanion --help` | Lists `spike`, `listen`, `maintenance` subcommands |
| `bookcompanion spike tts --help` | exits 0; `--book-id` `--section-id` `--voice` options |
| `bookcompanion listen --help` | exits 0; `--generate` `--scope` `--voice` options |
| `bookcompanion maintenance audio-positions-sweep --help` | exits 0 |
| `bookcompanion init --help` | exits 0; `--tts-engine` option present |

## Phase compliance summary

| Phase | FRs covered | Outcome |
|---|---|---|
| F1 — SettingsTtsPanel | FR-43/45/47 | Verified — 4 states reachable, panel mounted live |
| F2 — SpikeFindingsBlock | FR-44 | Verified — both available + unavailable code paths render |
| F3 — VoiceSampleButton | FR-45 | Verified — Web Speech + Kokoro paths covered by unit tests |
| F4 — KokoroStatusIndicator | FR-47 | Verified — 4 states + Download/Retry buttons |
| F5 — `bookcompanion spike tts` CLI | FR-50/D30 | Verified — help works; Jinja template + clip path wired |
| F6 — `init --tts-engine kokoro` | FR-06 | Verified — option visible; eager download path |
| F7 — `init` ffmpeg gate | FR-51 | Verified — gate triggers when engine=kokoro and ffmpeg missing |
| G1 — `listen <book_id>` | FR-48 | Verified — help works; afplay-based playback path |
| G2 — `listen --generate` | FR-49 | Verified — POST + SSE-stream path with Rich progress |
| G3 — `maintenance audio-positions-sweep` | FR-26b | Verified — orphan removal across all ContentType variants |
| H1 — Media Session metadata | FR-33/D34 | Verified — Mp3Engine.applyMediaSession sets metadata + playback handlers |
| H2 — `chapterInfo` for book summary | FR-33 | Verified — chapterInfo wired via MediaInfo.chapterInfo |
| H3 — RegenerateStaleAudioBanner | FR-66/MSF-N2 | Verified — banner hidden < 2 stale, mounted on LibraryView |
| H4 — Auto-advance | FR-34/D14 | Verified — useTtsEngine onEnd loads nextContentId when autoAdvance |
| H5 — Per-AudioFile size cap | NFR-13 | Verified — TooLargeError raised + AudioGenService emits `section_audio_failed{reason:"too_large"}` |

## Deferred / explicit gaps

These items are documented gaps, not skips:

1. **Kokoro live MP3 generation** — requires `kokoro-onnx` + `soundfile` deps not yet in `pyproject.toml`. Spike CLI invocation gracefully degrades to empty MP3 + writes findings template.
2. **Wireframe diff (full)** — visual style follows host app DESIGN.md tokens (intentional style adaptation per Phase 4 sub-step 3f rule). IA / copy / states verified live.
3. **NFR-01/02/03 latency benchmarks** — manual gates per plan TN; spec defers automated benchmarks to follow-up.
4. **`/api/v1/settings/tts/download` endpoint** — KokoroStatusIndicator references the route; backend implementation is a follow-up (status reads currently work; user can also `bookcompanion init --tts-engine kokoro`).

## Cleanup

- Verification server stopped (`kill $(lsof -ti:8765)`)
- `frontend/dist` left in place; `backend/app/static` updated as part of TN
- No test data created on disk

## Outcome

**Verified.** All Phase F + G + H tasks ship. 5 phases (A–E previously verified; F+G+H this pass) complete the audiobook-mode v1 feature.
