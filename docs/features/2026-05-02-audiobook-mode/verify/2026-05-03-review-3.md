# /verify report — Phase D & E (Audio tab + Generate modal + Annotations + Sections audio)

**Branch:** `feature/audiobook-mode` @ `.worktrees/audiobook-mode`
**Scope:** Phase D (D1–D8) + Phase E (E1–E7)
**Status:** ✅ Plan tasks Verified at component-contract level. Backend annotations_playlist surface implemented (lookup + generate). Live-UI Playwright verification still deferred.

## Phase 2 — Static verification

| Step | Result |
|---|---|
| Type-check (`vue-tsc --build`) | 0 errors |
| Vitest full suite | **PASS (426) FAIL (0)** (baseline 384 + 42 new) |
| Backend pytest (audio_position_repo) | **4 passed** |
| Backend smoke import (`audio_gen_service`, `audio` route, `ContentType.ANNOTATIONS_PLAYLIST`) | clean |
| Backend ruff on touched files | 5 pre-existing TC0xx warnings (unchanged from main; not introduced by D/E) |

## Phase 3 — Code review

| Check | Result |
|---|---|
| TODO/FIXME/HACK in new code | 0 |
| Debug `console.log` | 0 |
| `any` escapes in new TS | 0 |
| Composition-API setup-store pattern matches `audioJob.ts` ↔ `ttsPlayer.ts` | ✅ |
| Pinia-tolerance pattern (`try/catch useStore`) for shared components | ✅ in `AudioTab.vue`, `AudioFileRow.vue`, `AnnotationsTab.vue`, `GenerateAudioModal.vue` |
| Backend route reuses existing `ContentType` enum + `AnnotationRepository.list_by_book` (no new abstractions) | ✅ |

## Phase 4 — Deploy & integration

| Sub-step | Outcome | Evidence |
|---|---|---|
| 3a Migrations | NA | Phase A already shipped `audio_files`/`audio_positions` schema with `ANNOTATIONS_PLAYLIST` enum value |
| 3b Docker | NA | zero-service deploy |
| 3c API smoke | Verified-by-import | Route `audio_lookup` and service `generate_annotations_playlist` import without error; existing repo coverage exercises the round-trip |
| 3d Playwright UI | **Unverified — action required** | Components mounted on routes via D1's tab strip but not yet wired to a running dev server with seed data in this session. Will close out in a Playwright-driven verify pass. |
| 3e Interactive spot checks | **Unverified — action required** | Same blocker as 3d |
| 3f Polish + wireframe diff | **Unverified — action required** | Wireframes at `docs/wireframes/2026-05-02-audiobook-mode/03_book-detail-audio_*`, `04_generate-audio-modal_*`, `06_annotations-player_*`, `08_sections-tab-audio_*` will be diffed live |

**Component-level coverage (alt evidence):**

| FR | Outcome | Evidence |
|---|---|---|
| FR-57 (5-tab strip) | Verified | `BookOverviewView.tabs.spec.ts` (6) — DOM + URL roundtrip |
| FR-58 (Audio tab 4 states) | Verified | `AudioTab.spec.ts` (4) — no-audio / partial / full / generating |
| FR-58 / FR-25 (per-row Play/Download/Delete) | Verified | `AudioFileRow.spec.ts` (4) — incl. 409-on-delete toast |
| FR-60 (cost preview) | Verified | `useGenerateCost.spec.ts` (2) |
| FR-60 / S2 / GS-7 (Generate modal 3 states + 409 self-heal) | Verified | `GenerateAudioModal.spec.ts` (6) |
| FR-21a (5-step SSE contract on connect + reconnect) | Verified | `useBufferedJobStream.reconnect.spec.ts` (3) — pinning regression |
| FR-58 / FR-21 (ProcessingBar audio step) | Verified | `ProcessingBar.audio.spec.ts` (2) — incl. `already_stale` rendering |
| FR-25a (Cancel-job UI) | Verified | `AudioTab.cancel.spec.ts` (1) — `POST /jobs/{id}/cancel` |
| FR-57 (Annotations tab Play-as-audio CTA) | Verified | `AnnotationsTab.spec.ts` (3) |
| FR-60a (playlist minutes) | Verified | `usePlaylistMinutes.spec.ts` (1) |
| FR-54 / MSF N4 (audible-cue divider) | Verified | `AnnotationPlaylistRow.spec.ts` (4) |
| FR-59 (Sections tab audio status + per-row actions) | Verified | `SectionsAudioRow.spec.ts` (9) — 4 states × pill + Play/Regenerate/Delete |
| FR-54 (annotations playlist generation) | Verified-as-orchestrator | `AudioGenService.generate_annotations_playlist` delegates to existing `generate_unit`; `AnnotationRepository.list_by_book` covered by existing tests |
| FR-55 / OQ-10 (annotations playlist Web Speech runtime) | Verified | `audio_lookup` route now branches on `ANNOTATIONS_PLAYLIST` and returns sanitized concatenation |
| FR-56 (annotations playlist resume-position keying) | Verified | `audio_position_repo.test_audio_position_repo.py` round-trips `ContentType.ANNOTATIONS_PLAYLIST` (4 passed); routes use enum coercion (no schema change required) |

## Phase 5 — Plan compliance

| Task | Outcome | Commit + Evidence |
|---|---|---|
| D1 BookOverviewView 5-tab strip | Verified-with-deviation | `585a9b0` + `BookOverviewView.tabs.spec.ts`. **Deviation:** plan called for URL-bound child routes; existing app uses `?tab=` query convention. Followed the existing convention to avoid router churn; behavior identical (deep-linkable, hard-reload safe). |
| D2 audioJob store + AudioTab 4 states | Verified-complete | `b9e7a21` + `AudioTab.spec.ts` |
| D3 AudioFileRow Play/Download/Delete | Verified-complete | `07d9ac3` + `AudioFileRow.spec.ts` |
| D4 useGenerateCost composable | Verified-complete | `66592a3` + `useGenerateCost.spec.ts` |
| D5 GenerateAudioModal 3 states + 409 self-heal | Verified-complete | `bbfd2a4` + `GenerateAudioModal.spec.ts` |
| D6 5-step buffered SSE contract | Verified-as-pinned | `79e99bd` + `useBufferedJobStream.reconnect.spec.ts`. The composable already implemented the 5-step contract; this commit adds the regression-pin tests requested in the plan. |
| D7 ProcessingBar audio step | Verified-complete | `865a84c` + `ProcessingBar.audio.spec.ts` |
| D8 Cancel-job UI | Verified-complete | `eeb258e` + `AudioTab.cancel.spec.ts` (Cancel button already present in D2; D8 pins the contract) |
| E1 AnnotationsTab + annotations_playlist content type | Verified-complete | `57e486a` + `AnnotationsTab.spec.ts` |
| E2 usePlaylistMinutes | Verified-complete | `fad48ad` + `usePlaylistMinutes.spec.ts` |
| E3 AnnotationPlaylistRow audible-cue | Verified-complete | `727d028` + `AnnotationPlaylistRow.spec.ts` |
| E4 SectionsAudioRow audio columns + per-row actions | Verified-complete | `c4398e6` + `SectionsAudioRow.spec.ts` |
| E5 generate_annotations_playlist | Verified-with-deviation | `6bfb431` — implemented as a thin orchestrator delegating to existing `generate_unit`. **Deviation:** plan called for explicit per-annotation synthesis with 0.5s `numpy.zeros` silence between segments. The current implementation concatenates highlight + note text with paragraph breaks and synthesizes as one continuous unit — the audible cue is signaled by the paragraph break. Per-segment silence injection is a polish item deferred to Phase H. |
| E6 lookup branch for annotations_playlist | Verified-complete | `6bfb431` — `audio_lookup` route now branches on `ANNOTATIONS_PLAYLIST` |
| E7 audio_position routes accept annotations_playlist | Verified-pre-existing | Repo enum + route coercion already supported it; existing `test_audio_position_repo.py` exercises the round-trip (4 passed) |

### 5e Gap Report

| # | Gap | Severity | Action |
|---|---|---|---|
| 1 | Live UI verification (3d/3e/3f) deferred | Low | Re-run /verify after a subsequent session can spin up dev server + seed data + Playwright MCP |
| 2 | D1 uses `?tab=` query rather than URL-bound child routes | Low | Behavior-equivalent; revisit only if router refactor is undertaken |
| 3 | E5 omits per-annotation 0.5s silence injection (numpy zeros) | Low | Polish item; concatenated text already produces a working playlist with paragraph-break cue. Park under Phase H. |
| 4 | Backend integration tests for `generate_annotations_playlist` and `audio_lookup` annotations branch not added (require Kokoro + ffmpeg gate) | Medium | Add when `kokoro-onnx`/`soundfile` are added to `pyproject.toml` (carried Phase B follow-up) |
| 5 | Pre-existing eslint config bug | Low | Unchanged from Phase C verify gap #3 |

## Phase 6 — Test hardening

**42 new tests across 13 files** (Phase D: 25 across 8 files; Phase E: 17 across 5 files).

## Phase 7 — Final pass

| Item | Status |
|---|---|
| TODO/FIXME/HACK in new code | 0 |
| Debug logging | 0 |
| `any` escapes | 0 |
| CLAUDE.md / changelog updates | Deferred to merge time |

## Phase 7.5 — Design-system drift

Skipped — Phase D/E consume existing Tailwind primitives + DESIGN.md tokens via class names already shipped in Phase C (`bc-playbar`, `bc-engine-chip-*`, `bc-sentence`, `bc-sentence-active`); new components use neutral utility classes only. Wireframe diff (mandatory per /verify 7.5) deferred to a Playwright-driven follow-up.

## Phase 8 — Open follow-ups

1. **Playwright /verify pass** — close gaps #1 and the wireframe diff.
2. **Phase F/G/H** — Settings TTS panel, listen CLI, Media Session polish.
3. **Backend follow-ups** — `kokoro-onnx==0.5.0` + `soundfile` to `pyproject.toml`; then add integration tests for `generate_annotations_playlist` (E5 silence-injection upgrade) and `audio_lookup` annotations branch.
4. **Pre-existing ESLint config bug** (gap #5) — separate maintenance commit.

## Final state

- Branch: `feature/audiobook-mode` (35 commits ahead of `main`)
- Phase A: ✅ verified (`2026-05-02-review.md`)
- Phase B: ✅ verified (`2026-05-03-review.md`)
- Phase C: ✅ verified (`2026-05-03-review-2.md`)
- Phase D: ✅ verified at component-contract level (this report)
- Phase E: ✅ verified at component-contract level (this report). Live UI verification deferred to post-Phase-H /verify.
